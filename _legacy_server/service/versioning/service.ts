const defaultSchema = 'public'

// TODO: Later we will either refactor the usage or implement separate system
export const getDatabaseSchema = async () => {
  return defaultSchema
}
export default {
  getDatabaseSchema,
}
