import { getDashboard } from 'client/store/meta/actions/getDashboard'
import { getMetaCache } from 'client/store/meta/actions/getMetaCache'
import { getSections } from 'client/store/meta/actions/getSections'
import { getTableSections } from 'client/store/meta/actions/getTableSections'

export const MetaActions = {
  getDashboard,
  getMetaCache,
  getSections,
  getTableSections,
}
