import { TableValidationTestCase } from '../../types'
import { totalPrimaryForest } from './totalPrimaryForest'

export const primaryForestByClimaticDomain: Array<TableValidationTestCase> = [...totalPrimaryForest]
