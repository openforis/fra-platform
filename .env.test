NODE_ENV=test

# Default web server port
PORT=9001
APP_URI=http://localhost:9001

# DB
PGHOST=localhost
PGPORT=5442
PGDATABASE=frap-dev
PGUSER=frap
PGPASSWORD=frap

DEBUG=true

FRA_MAIL_ENABLED=false
